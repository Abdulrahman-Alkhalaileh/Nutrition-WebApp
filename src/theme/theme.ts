import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#f3e5f5",
      dark: "#262626",
      light: "#f3e5f5",
    },
    secondary: {
      main: "#edf2ff",
    },
  },
});
